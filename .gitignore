# --- generated dataset / data (never commit) ---
/data/

# --- Python virtualenv + bytecode ---
.venv/
venv/
env/
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- runtime logs ---
*.log
*.err
/corpus_run.log
/render_run.log

# --- packaged inference bundle: model weights are 128MB of binaries. Kept out of git history
# (irreversible bloat); ship them alongside a release or copy from data/checkpoints/.
# The tokenizer (~470KB) IS committed since the bundle is unusable without it.
/inference_v1/assets/checkpoints/

# --- generated galleries + ad-hoc visual checks (regenerate: build_layout_gallery.py,
# build_data_formulation.py) ---
/data_formulation.html
/layout_gallery.html
/_r*_worksheet_check*.jpg

# --- OS / editor ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# --- secrets / caches ---
.env
.env.*
*.token
hf_token*
.cache/
.huggingface/

# --- claude-code local ---
.claude/

# detector debug visualizations
detected_*.jpg

# --- archived ad-hoc debug scripts/artifacts + tool outputs (V5 cleanup) ---
/_debug_archive/
/_dataset_viz*/
/_browse*/
